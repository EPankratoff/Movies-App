import { Input } from 'antd'
import React from 'react'

function SearchFilm() {
  return <Input style={{ width: '100%', margin: '0 auto' }} placeholder="Type to search..." />
}

export default SearchFilm
